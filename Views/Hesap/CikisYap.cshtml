@{
    ViewData["Title"] = "Çıkış Yap";
}

<div class="container mt-5 text-center">
    <h3 class="mb-4">Çıkmak istediğinize emin misiniz?</h3>
    <form asp-action="CikisYap" method="post">
        @Html.AntiForgeryToken()
        <button type="submit" class="btn btn-danger me-2">Çıkış Yap</button>
        <a href="@Url.Action("Index", "Home")" class="btn btn-secondary">İptal</a>
    </form>
</div>
